# ~/.aws/config — LocalStack-only dev (matches this project's default setup)
#
# [default]
# region = us-east-1
# endpoint_url = http://localhost:4566
#
# ~/.aws/credentials
# [default]
# aws_access_key_id = test
# aws_secret_access_key = test
#
# Real AWS later (separate profile):
# [profile resilience-aws]
# region = af-south-1
# # no endpoint_url
